- clean: ['~']

- link:
    ~/.aliases: aliases
    ~/.gdbinit: gdbinit
    ~/.gitconfig: gitconfig
    ~/.tmux.conf: tmux.conf
    ~/.vimrc: vimrc
    ~/.zsh: zsh/
    ~/.zshrc: zshrc
    ~/.zshenv: zshenv

- shell:
  - [git submodule update --init --recursive, Installing submodules]

