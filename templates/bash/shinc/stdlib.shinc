__currdir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

source "${__currdir}/debug.shinc"

source "${__currdir}/color.shinc"
initializeANSI

source "${__currdir}/prog.shinc"
source "${__currdir}/error.shinc"
source "${__currdir}/util.shinc"
source "${__currdir}/string.shinc"

unset __currdir
