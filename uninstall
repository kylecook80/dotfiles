#!/bin/bash

DEFAULT_FILES="aliases gdbinit gemrc gitconfig pryrc screenrc tmux.conf vimrc zshenv zshrc zsh"

for item in $DEFAULT_FILES; do
  if [[ -d "$item" ]]; then
    rm -rf $HOME/.$item
  else
	  rm $HOME/.$item
  fi
done
